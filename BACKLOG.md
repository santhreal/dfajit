# BACKLOG - dfajit
DONE 2026-07-14 src/dfa.rs:170 | silent-fallback | high | Bypasses JIT compilation on x86_64 by unconditionally calling the interpreted fallback, making the JIT compiler dead code | FIXED: enabled x86_64 JIT in `src/dfa.rs::compile_with_output_links`. Found and fixed two JIT bugs: (1) `src/codegen/compile.rs` transition lookup used REX prefix `0x42` (index=r10, base=rdx) instead of `0x41` (base=r10, index=rdx), causing a segfault; (2) `imul rdi, rdi, 16` assumed `Match` was 16 bytes but `matchkit::Match` is 12 bytes, corrupting second and later matches. Added per-table JIT-vs-interpreted parity self-check (`verify_jit_parity` / `parity_inputs`) that compiles both scanners and compares `scan_count` + first 64 `scan` matches on static edge cases and BFS-generated shortest accept paths; fails closed with new `Error::JitParity` on any divergence. Set `from_regex_patterns` literal pattern lengths to the source byte length so regex-literal start offsets are correct. `cargo +1.92.0 test -p dfajit` 334 passed (CARGO_EXIT=0); `cargo +1.92.0 test -p dfajit --features regex --lib` 41 passed. | status=done
DONE 2026-07-14 | src/dfa.rs:411 | bug | high | Aho-Corasick builder constructs output links before propagating suffix patterns, causing inherited-pattern states to miss nested suffix matches | FIXED: the propagation loop in `build_failure_links` now sets each inherited state's `output_link` to the nearest ancestor's `output_link` (not the ancestor itself), so the scan continues down the suffix chain without duplicating the inherited pattern. The explicit `output_link` chain is still built before propagation; inherited links are filled in afterward. Proving test `test_inherited_state_emits_nested_suffix_matches` with patterns [abcd, bc, c] on input "xabc" reports both bc and c. | status=done
src/table/minimize.rs:47 | bug | high | DFA minimization ignores high bit 31 set on transitions, causing incorrect partition signature mapping to class 0 | FIXED: mask with STATE_MASK before partition lookup (a deserialized table via from_table may carry ACCEPT_FLAG). Added pub(crate) ACCEPT_FLAG/STATE_MASK consts in table/mod.rs (ONE-PLACE: also repointed dfa.rs:116/125/220/292, serialize.rs:196, buffer.rs:108/144 off inline literals). Proving test tests_advanced::minimize_preserves_accept_flag_on_transitions. lib minimize suite 8 green. | status=done
src/table/minimize.rs:90 | bug | high | DFA minimization drops transition high bit 31 and redirects targets to state 0 during transition table reconstruction | FIXED (same edit): mask STATE_MASK to find representative target, re-apply ACCEPT_FLAG when set on the original (target accept-ness survives minimization). Same proving test asserts flagged transition points to accept class, not state 0. | status=done
src/table/analysis.rs:17 | bug | medium | Slicing transitions row in compute_ranges can panic if transitions vector length is modified and row_start exceeds row_end | FIXED: row_start is now `.min(self.transitions.len())` so it can never exceed row_end (which is already min'd to the length). On a well-formed table (len == state_count*class_count) it is a no-op; on a short/corrupt table it yields empty rows for out-of-range states instead of panicking the slice. Proving test compute_ranges_does_not_panic_on_short_transition_buffer (new(4,256) truncated to 10 -> ranges[1..] all empty, no panic). | status=done
src/table/mod.rs:109 | capability | medium | Exposing raw mutable transitions vector via transitions_mut allows breaking the table size invariants | FIXED (wrap-to-enforce, done properly): changed the public `transitions_mut(&mut self) -> &mut Vec<u32>` to `-> &mut [u32]` (a SLICE), so the supported API structurally cannot push/truncate = the `len() == state_count*class_count` invariant the scanner relies on can no longer be broken through it (element/target rewrites still allowed; the scanner already bounds-checks targets so a bad target fails closed, not OOB). The length-corrupting capability is preserved ONLY for the adversarial/regression harness via a new `#[doc(hidden)] pub fn transitions_raw_mut(&mut self) -> &mut Vec<u32>` (kept out of the advertised API, documented as invariant-bypassing test-only). Repointed the 3 length-mutating test sites (tests_core.rs invalid_table_size_rejected truncate, tests/integration/error_handling.rs push, tests/regression/bug_test.rs truncate) to transitions_raw_mut; the ~6 element-mutation sites (`transitions_mut()[0]=...`) work unchanged via slice indexing. Proving test transitions_mut_allows_element_edit_but_preserves_size_invariant (element edit reflected, len unchanged, table still compiles). NOTE minor breaking change to a footgun accessor, but grep found NO production/external caller (only tests) and element-mutation callers are source-compatible; only push/truncate callers break, which is exactly the invariant-violation being removed from the public API (Law 3: no legitimate caller loses capability). | status=done
src/codegen/compile.rs:242 | perf | low | Interpreted fallback allocates executable page memory via mmap and mprotect syscalls unnecessarily | FIXED: compile_interpreted_fallback no longer mmaps a page, writes a lone RET (0xC3), mprotects it PROT_EXEC, and munmaps it at Drop - that whole exec-page dance (2 syscalls + an unused RX page) was dead because the interpreted scan paths (scan_interpreted/scan_count_interpreted in buffer.rs) dispatch on is_jit==false and use only table/accept_pattern/output_links; `ptr` is transmuted+called ONLY on the is_jit==true JIT path. Now returns ExecutableBuffer { ptr: null_mut(), len: 0, is_jit: false, ... }. Verified safe: ExecutableBuffer::Drop already guards `!ptr.is_null() && len > 0` (no munmap on null/zero), and no non-JIT path dereferences ptr. Removed the now-dead FALLBACK_CODE/page_size locals; Error/libc stay used by the real JIT path so no unused-import fallout. Signature kept as Result (now always Ok) for caller compatibility (Law 3). Since interpreted is the DEFAULT scanner (dfa.rs:170), this removes 2 syscalls + a page alloc from every compile. Verified via full dfajit test suite. | status=done
DONE 2026-07-14 | perf | medium | JIT assembler reloads the transition table address into rdi inside the hot scan loop on every byte | Load the transition table address once outside the scan loop and preserve it in rdi | status=done | FIXED: hoisted the transition table base load out of the hot scan loop in `src/codegen/compile.rs`. The base is now loaded once into the `r10` register before `scan_top`; the per-byte lookup uses `mov eax, [r10 + rdx*4]` (4 bytes) instead of reloading `rdi` with `movabs rdi, <trans_base>` every iteration. `r10` is scratch but preserved across the loop because the JIT scanner makes no calls. Full dfajit suite green (CARGO_EXIT=0), including `jit_parity_*` and `targeted_regressions::input_100mb_with_sparse_markers_counts_every_match`.
src/dfa.rs:342 | perf | low | Trie construction in from_patterns allocates memory without state count bounds checking, only validating after completion | FIXED: from_patterns now checks `state_count >= TransitionTable::MAX_STATES` before each new-state trans.push (each row is 1 KiB), returning Error::TooManyStates immediately instead of allocating the entire oversized trie (potentially GBs) before build_dense_table's post-hoc TransitionTable::new check rejects it. Proving test from_patterns_fails_fast_past_max_states (pattern of MAX_STATES+8 bytes -> Err(TooManyStates{states>max})). | status=done